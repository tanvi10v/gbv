export const preloadAssets = (instance) => {
    instance.load.image('startBackground', 'assets/startBg.png'); // Background image
    instance.load.image('startButton', 'assets/play.png'); // Button image
}